# Database URL (traffic_db is the name we just created)
spring.datasource.url=jdbc:mysql://localhost:3307/traffic_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

# Your credentials
spring.datasource.username=root
spring.datasource.password=Hasangi123@

# Hibernate settings (This automatically creates your tables based on your Java code)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect